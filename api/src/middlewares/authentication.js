import jwt from "jsonwebtoken";
import config from "../config";

function authenticateToken(req, res, next) {
  // Implement the same authentication middleware as before
}

export default {
  authenticateToken,
};
