class User {
    constructor(id, firstName, lastName) {
      this.id = id;
      this.firstName = firstName;
      this.lastName = lastName;
    }
  
    // You can add any additional methods or properties specific to the User model here
  }
  
  module.exports = User;
  