class Show {
    constructor(id, name, shortDescription, longDescription, meta) {
      this.id = id;
      this.name = name;
      this.shortDescription = shortDescription;
      this.longDescription = longDescription;
      this.meta = meta;
    }
  
    // You can add any additional methods or properties specific to the Show model here
  }
  
  module.exports = Show;
  